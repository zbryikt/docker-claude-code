# docker-llmcli

run llm cli in docker.

usage:

    # build docker image
    docker build -t llmcli .

    # enter container
    ./run.sh

    # in container, start claude:
    claude


## License

MIT
