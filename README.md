# docker-claude-code

run claude code in docker.

usage:

    # build docker image
    docker build -t claude-code-cli .

    # enter container
    ./run.sh

    # in container, start claude:
    claude


## License

MIT
